#!/bin/sh

PIDFILE="$XDG_RUNTIME_DIR/wallgif.pid"


# kill the last pid
if [ -f "${PIDFILE}" ]; then
  head "${PIDFILE}" | xargs kill -9 >/dev/null 2>&1
  rm -f "${PIDFILE}"
fi

# get screen resolution
