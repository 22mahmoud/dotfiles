[user]
  name = Mahmoud Ashraf
  email = mahmoud.ashraf@roadtripnation.org
[commit]
  verbose = true
[color]
  ui = true
[core]
  pager = delta
  editor = nvim
[difftool]
  trustExistCode = true
[interactive]
  diffFilter = delta --color-only
[push]
  followTags = true
[merge]
  tool = nvimdiff4
[mergetool "nvimdiff4"]
  cmd = nvim -d $LOCAL $BASE $REMOTE $MERGED -c '$wincmd w' -c 'wincmd J'
[log]
  decorate = short
  date = local
[delta]
  side-by-side = false
  line-numbers-left-format = ""
  line-numbers-right-format = "│ "
  syntax-theme = "base16"
[pull]
  rebase = true
[init]
  defaultBranch = main
[alias]
  a = add
  b = branch
  c = commit
  cl = clone
  co = checkout
  cb = checkout -b
  cp = cherry-pick
  m = merge
  pu = push
  pl = pull
  r = reset
  s = status
  undo = "!git reset --soft HEAD^ && git reset all"
	l = log --color=always --graph --pretty=format:\"%C(blue)%h %Creset- %C(green)(%cr) %Creset%s - %C(cyan)%aN %C(magenta)%d\" --date=auto:human

